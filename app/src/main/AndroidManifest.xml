<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dayal.coordinatorapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPager_tabs" />
        <activity android:name=".CollapsingToolbarFab" />
        <activity android:name=".ToolbarTabs" />
        <activity android:name=".DemoFragment" />
        <activity android:name=".DemoAdapter" />
        <activity android:name=".DemoItem" />
        <activity android:name=".Space" />
        <activity android:name=".DemoItemHolder" />
        <activity android:name=".ViewPagerAdapter"></activity>
    </application>

</manifest>